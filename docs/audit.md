# AUDIT — 2026-08-19

Scope: code vs. claims in `docs/architecture.md`, `docs/compliance.md`, `README.md`, `CLAUDE.md`.
`npm run test-all` passes green in ~10s (tsc, lint-layers, fixture, smoke, build).

## DEMO_KILLERS (fix before rehearsal)

- [x] **The central pitch claim is false: Briefing Mode, Review and Lock are gated only by a disabled HTML attribute, not by document status.** `$('btn-brief').onclick`, `$('btn-review').onclick` and `$('btn-lock').onclick` in `src/ui/app.ts:204-206,466-470` call `S.do(...)` / `brief.start()` unconditionally — they never call `statusGate()` inside the handler. Only `$('btn-export').onclick` (`src/ui/app.ts:479-481`) re-checks `gate.exportAllowed`. Opening devtools and running `document.getElementById('btn-brief').disabled=false` then clicking it reaches Briefing Mode on a `DRAFT`, unscaled-if-you-also-clear-scale-first document. Worse: `apply()`'s `SET_STATUS` case (`src/core/ops.ts:139-143`) performs **no transition validation at all** — it accepts any status string in the payload, so a console call `S.do('SET_STATUS',[],{status:'LOCKED'},'x')` jumps `DRAFT → LOCKED` directly, skipping the review gate entirely, no devtools attribute editing even required. This is exactly the scenario `docs/architecture.md:507` and the sentence-to-defend (`docs/architecture.md:815-819`) claim cannot happen. — `src/ui/app.ts:204-206`, `src/ui/app.ts:466-470`, `src/core/ops.ts:139-143` — fix: re-check `statusGate(...)` inside each handler (like export does) **and** validate the legal transition set inside `apply()`'s `SET_STATUS` case so the op itself refuses `DRAFT→LOCKED` and any bypass reachable only through the UI layer. — **FIXED**: `apply()`'s `SET_STATUS` case now validates against `LEGAL_STATUS_TRANSITIONS` (`DRAFT→REVIEWED→LOCKED`, forward only, throws on anything else) in `src/core/ops.ts`; `btn-review`/`btn-lock`/`btn-brief` handlers re-check `statusGate`/`S.doc.status` before calling `S.do`/`brief.start()` in `src/ui/app.ts` and `src/ui/briefingExport.ts`.

## JUDGE_TRAPS (fix before submission)

- [x] `docs/compliance.md:18` marks `¶3 "export them to a file"` as **COVERED** citing `.sitepkg (encrypted) + GLB`. No `.sitepkg` export exists anywhere in the code — only GLB (`src/ui/exportGlb.ts`) and the op log as `.jsonl` (`src/ui/app.ts:495-503`). `docs/architecture.md:787` itself correctly lists encrypted `.sitepkg` as **DEFERRED** ("put this back first"). The two docs contradict each other, and compliance.md is the one most likely to be read clause-by-clause. — `docs/compliance.md:18` vs `docs/architecture.md:787` — fix: change compliance.md's status for this row to PARTIAL and name GLB + ops.jsonl only, or ship the 30-line `.sitepkg` before submission. — **FIXED**: row now reads PARTIAL, names GLB + `ops.jsonl` only, and points at `architecture.md` §9.
- [x] Technical entity IDs are shown directly in commander-facing panels, not just the audit log: the bridges list renders `e.opening_id` (e.g. `f-o-d4`) as the primary label, and the room selection panel prints raw `betweenness ${n}` and `origin ${o.origin}` strings. A judge reading the UI as a commander tool will hit these immediately. — `src/ui/app.ts:315-318` (bridges list), `src/ui/panels.ts:126-149` (renderSelection) — fix: give doors/rooms human labels in the commander-facing lists and move raw metric names into a clearly-marked "technical" sub-panel. — **FIXED**: bridges/critical-rooms lists now lead with the human label and a plain-language reason (`src/ui/app.ts` `renderAnalysers`), with raw ids/scores moved to a `.tech` line prefixed "technical:"; `renderSelection` in `src/ui/panels.ts` does the same.
- [x] `app.ts` is 515 lines. `CLAUDE.md` §6 states "Files under ~400 lines" as a hard standard the team can be asked to defend line-by-line. This file is the one most likely to be opened live if a judge asks "walk me through your event wiring." — `src/ui/app.ts` (515 lines) — fix: split scale-panel wiring, routing wiring, and briefing/export wiring into separate `ui/` modules, or acknowledge the miss before a judge finds it. — **FIXED**: split into `src/ui/scalePanel.ts` (101 lines), `src/ui/routing.ts` (110 lines), `src/ui/briefingExport.ts` (70 lines); `app.ts` is now 355 lines.
- [x] The fixture is 100% `origin: HUMAN` (41/41 walls, 29/29 openings, 13/13 rooms) — confirmed by direct count. `docs/architecture.md` and `README.md` never claim otherwise, but nothing in the UI states this plainly either; if a judge asks "what fraction came from CV," the honest answer ("all of it — perception is deferred") must be said before the confidence/origin panel is shown, or the origin badges themselves invite the question. — `fixtures/site.json` — fix: rehearse this exact answer; optionally add one line to the boot screen or briefing script stating perception is deferred. — **FIXED**: net/status panel now shows "Perception: DEFERRED — every element below is HUMAN origin" (`src/ui/app.ts` `refreshNet`). Still rehearse the answer live.

## PAPER_CUTS (fix if there is time)

- [x] There is no persistence path by design (`CLAUDE.md` forbids `localStorage`; `docs/architecture.md:746-749` calls this an open risk). A browser refresh mid-review silently loses the entire op log and all edits with no warning dialog — confirmed by reading `src/ui/app.ts` and `src/main.ts`: `loadDoc()` always re-reads the bundled fixture, nothing intercepts `beforeunload`. Not a code bug (it's the documented architecture), but worth a `beforeunload` confirm dialog so a stray Cmd+R doesn't silently erase 10 minutes of stage work. — `src/main.ts`, `src/ui/app.ts` — fix: add a one-line `beforeunload` handler that warns if `S.ops.length > 0`. — **FIXED**: added in `src/ui/app.ts`.
- [x] `docs/compliance.md:22` calls the install-time asset layer "STRONG," but `docs/architecture.md:620-627` marks the same layer **DEFERRED** and says the area pack is hand-authored, not fetched. Minor doc-internal inconsistency in tone/status between the two files for the same capability. — `docs/compliance.md:22` vs `docs/architecture.md:620` — fix: align wording; "STRONG" should describe the kill-switch (7.2/7.3), not the deferred install-time fetcher. — **FIXED**: row now reads DEFERRED for the fetcher, with the STRONG claim reserved for the kill-switch row above it.
- [x] The 2D plan had no zoom — `fit()` force-fit the whole storey to the canvas on every draw with no way to inspect detail. — `src/view2d/plan.ts` — **FIXED**: added cursor-anchored wheel zoom (`wheel()`) and double-click-to-reset (`resetView()`), reset automatically on storey-tab switch.
- [x] Briefing Mode's "Assigned route" step silently reported `0 doors` when a target was selected but actually unreachable (`route === null`), rather than saying so — the demo's centrepiece would have shown a confident, wrong "route" for a disconnected space. — `src/ui/briefing.ts` — **FIXED**: step body now distinguishes "no target selected" from "NO ROUTE — disconnected", styled as a warning (`.b-warn`) in `src/ui/style.css`. The route-panel and 3D/plan view carry the same NO ROUTE state: `src/ui/routing.ts` toggles `#no-route-banner` over the 3D view and renders `route-out` as a blocking item instead of the neutral `.empty` style previously shared with benign "nothing to report" states.

## VERIFIED (things the docs claim that I confirmed work)

- Scale engine has three states (`UNSCALED`/`PROVISIONAL`/`VALIDATED`) and `UNSCALED` genuinely blocks export via `statusGate.exportAllowed` (blocking findings include `id:'unscaled'`) — `src/core/scale.ts`, `src/core/site.ts:113-125,192`.
- Kill-switch blocks non-loopback `fetch`/`XHR`/`WebSocket`/`EventSource`/`sendBeacon` and explicitly permits `127.0.0.1`/`localhost`/`file:`/`data:`/`blob:` — `src/core/netguard.ts:34-57`.
- `lint-layers.mjs` enforces both the layer-import rule and a network-API ban below the app layer, with a named, minimal allowlist — `tools/lint-layers.mjs`.
- Stairs are real stepped solids (looped `boxAt` calls per tread from `riser_m`/footprint), not coloured prisms — `src/geometry/build3d.ts:190-227`.
- OSM neighbourhood extrusion is real `ExtrudeGeometry` per footprint at `levels × 3.1 m`, not a placeholder — `src/geo/neighbourhood.ts:32-41`.
- Op log: `apply()` is pure, `Session.do` is the only writer, `verifyChain()` replays from origin and is exercised in the smoke test (`chain intact: true`) — `src/core/session.ts:44-100`, confirmed live in `npm run test-contracts` output.
- Bridge/articulation/route/betweenness/isolation/dead-end analysers produce correct, labeled output on the fixture, matching the "door=edge/bridge, room=articulation-point" distinction the architecture doc insists on — verified via `tools/smoke.ts` output.
- `origin`/`verified`/`confidence` are populated on every wall, opening, room, stair in the fixture (schema in `src/core/types.ts`, values confirmed by direct count) — I7 holds.
- Every `Finding` carries an anchor (`storey_id` plus one of `wall_id`/`room_id`/`opening_id`/`node_id`) — `src/core/site.ts:18-28`. I8 holds.
- "156 lines for Briefing Mode" is now stale: the NO ROUTE fix above grew `src/ui/briefing.ts` to 163 lines. Update the pitch line before it's quoted on stage.
- `npm run test-all` is green end-to-end (types, layer lint, fixture, headless pipeline+determinism, production build) in ~10s on this machine.

## UNVERIFIABLE (things I could not test in this environment)

- Actual demo-hardware boot time / first-load-with-cold-cache — only measured on this dev machine (`bootMs` printed live but not benchmarked against a fresh Node install or the actual demo laptops).
- Browser-based interaction (clicking through the UI, devtools bypass exploit executed live rather than read from source) — not run in a real browser this session; the bypass is a static-analysis finding from the handler code, not an observed click-through.
- Whether a laptop that has never had Node installed can build/run this — not tested; only source and lockfile presence were checked.
