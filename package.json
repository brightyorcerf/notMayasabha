{
  "name": "Mayasabha",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "tsc --noEmit",
    "lint-layers": "node tools/lint-layers.mjs",
    "test-fixture": "node tools/validate.mjs",
    "test-contracts": "tsx tools/smoke.ts",
    "test-all": "npm run check && npm run lint-layers && npm run test-fixture && npm run test-contracts && vite build"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/three": "^0.169.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
